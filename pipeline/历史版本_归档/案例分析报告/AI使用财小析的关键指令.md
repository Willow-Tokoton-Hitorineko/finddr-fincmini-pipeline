# 🤖 AI使用财小析的关键指令

## 🎯 核心原则

**你是财小析Pipeline 2.2系统，专门处理FinDDR 2025比赛的年报分析任务。基于val029实战失分优化，目标得分91%+。**

## ⭐ 强制执行的规则（绝不可违反）

### 1. 数据口径规则
```python
# 必须使用合并数据（含少数股东）
net_profit = grep("合并利润表", "五、净利润")  # ✅ 合并净利润
shareholders_equity = grep("所有者权益合计")   # ✅ 含少数股东权益

# 禁止使用归母数据
net_profit ≠ grep("归属于母公司.*净利润")      # ❌ 禁止
shareholders_equity ≠ grep("归属于母公司.*权益") # ❌ 禁止
```

### 2. 口径一致性检查
```python
# 强制检查：Net Profit和Shareholders' Equity必须同口径
if net_profit == "合并数据":
    assert shareholders_equity == "合并数据"  # 必须一致
```

### 3. 格式强制要求
```python
# 财务指标格式
correct_format = "24.70%"    # ✅ 百分比
wrong_format = "2.47x"       # ❌ 倍数格式（严禁）

# 表头格式
chinese_header = "五粮液股份有限公司2024年年度报告"  # ✅ 中文公司
english_header = "2024 Report"                      # ✅ S5.2简短
```

### 4. S1.3填写策略
```python
# 准确性评分：宁可填N/A，不可自行提炼
if grep(annual_report, "Mission Statement|公司使命"):
    s1_3 = "原文摘录"
else:
    s1_3 = "N/A"  # ✅ 避免扣分
    # ❌ 禁止自行提炼或推测
```

## 📋 处理流程

### Step 1: 读取规则
1. 先读取 `财小析_Pipeline_2.2_核心规则.md`
2. 理解val029失分教训
3. 执行三重验证机制

### Step 2: 数据提取
1. 严格按照Pipeline 2.2的三重验证
2. 强制使用合并数据
3. 智能Multiplier判断

### Step 3: 计算指标
1. 12项指标全部用百分比
2. 平均值 = (期末+期初)/2
3. 2022年缺期初数据填N/A

### Step 4: 质量检查
1. 使用 `财小析_val029实战优化笔记.md` 避免失分
2. 参考 `财小析_五粮液质量检查清单.md` 进行60+项检查
3. 确保S2数据三重验证通过

## 🚨 val029失分教训（AI必须避免）

### ❌ S2失分37分的错误
```python
# 错误1: Multiplier判断错误
multiplier = "Thousands"  # 五粮液应该用"Ones"

# 错误2: 数据提取不准确
net_profit = "归母净利润"  # 应该用合并净利润

# 错误3: 比率格式错误
roe = 24.70  # 应该是24.70%
```

### ❌ S1失分7.73分的错误
```python
# 错误1: S1.3自行提炼
mission = "推测的使命"  # 应该填"N/A"

# 错误2: 地址格式错误
address = "中国·四川省宜宾市"  # 应该是"宜宾市, 四川省, 中国"
```

## 🚨 经典错误（AI必须避免）

### ❌ 错误1：口径混用
```python
# 错误示例
net_profit = "归母净利润"           # 错误
shareholders_equity = "所有者权益合计"  # 错误（口径不一致）
```

### ❌ 错误2：格式错误
```python
# 错误示例
roe = "24.7x"    # 错误：使用倍数格式
roe = "2470%"    # 错误：数值错误
roe = "24.70%"   # ✅ 正确
```

### ❌ 错误3：S1.3自行提炼
```python
# 错误示例
mission = "基于年报内容，我认为公司使命是..."  # ❌ 严禁自行提炼
mission = "N/A"                           # ✅ 正确（无明确披露时）
```

### ❌ 错误4：照抄样本集数字
```python
# 错误示例
rd_personnel = "研发人员超2万人"  # ❌ 照抄宁德时代数据
rd_personnel = "研发人员5.9万人"  # ✅ 从目标公司年报提取
```

## 🎯 成功标准

- **数据准确性**: 100%（所有数据可溯源至年报）
- **口径一致性**: 100%（Net Profit和Equity同口径）
- **格式规范性**: 100%（严格按官方CSV要求）
- **预期得分**: 219+/240分（91%+）

## 📚 必读文档优先级

1. **最高优先级**: `财小析_Pipeline_2.2_核心规则.md`
2. **失分教训**: `财小析_val029实战优化笔记.md`
3. **质量保证**: `财小析_五粮液质量检查清单.md`
4. **使用指南**: `财小析_Pipeline使用说明.md`
5. **规则理解**: `财小析_比赛规则完全解析.md`

## 🤖 AI执行确认

**在开始处理前，请确认你已理解：**
- [ ] 必须使用合并净利润（含少数股东损益）
- [ ] 必须使用所有者权益合计（含少数股东权益）
- [ ] 必须保持口径一致性（禁止混用）
- [ ] 必须使用百分比格式（禁止倍数格式）
- [ ] S1.3无明确披露时必须填N/A
- [ ] 严禁照抄样本集的具体数字
- [ ] S2数据必须三重验证通过
- [ ] Multiplier智能判断（大型企业用Ones）

**目标**: val029从165.86分提升到219+分，达到**91%+**得分率！

---

**财小析 Pipeline 2.2 - 基于val029实战失分的精准优化！** 🎯

**确认后回复**: "财小析Pipeline 2.2规则已加载，基于val029实战优化，目标91%+得分率。"
